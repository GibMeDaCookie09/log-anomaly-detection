# core
drain3>=0.9.11
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3

# self-monitoring: reads the service's own logs back out of CloudWatch and
# archives batches to S3. Imported lazily, so nothing pays for it at import time.
boto3>=1.34

# service
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.0

# optional: local neural embeddings (large - torch)
# sentence-transformers>=2.5

# optional: hosted LLM / embeddings
# anthropic>=0.40

# dev
pytest>=8.0
